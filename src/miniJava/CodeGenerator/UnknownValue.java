package miniJava.CodeGenerator;

public class UnknownValue extends RuntimeEntity{
	
	public UnknownValue(int size, int address){ //address of undetermined value, eval at runtime...
		super(size,address);
	}
}
